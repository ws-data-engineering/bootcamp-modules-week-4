// TODO: What will the following line of code log?
console.log(window);

// The window.console property returns a reference to the console object, which provides methods for logging information to the browser's console.

// TODO: What will the following line of code log?
console.log(window.document);

// The window.document property returns a reference to the document contained in the window. *HTML

// TODO: What will the following line of code log?
console.log(document.documentElement);

// The documentElement read-only property of the document interface returns the element that is the root of the document.

// TODO: What will the following line of code log?
console.log(document.head);

// The head read-only property of the document interface returns the <head> element of the current document.

// Bonus

const bonus = window.length;
console.log(bonus);